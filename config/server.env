# Environment variables for the server container

# Path configurations
OQS_SSH_DIR=/opt/oqs-ssh
LIBOQS_DIR=/opt/liboqs

# Installation prefix for binaries and libraries
INSTALL_PREFIX=/usr/local

# Repository URLs
OQS_SSH_REPO=https://github.com/open-quantum-safe/openssh-portable.git
LIBOQS_REPO=https://github.com/open-quantum-safe/liboqs.git

# SSH configuration
NEW_SSH_PORT=2222
SSHD_USER=sshd
SSHD_GROUP=sshd

# Backup directory for existing SSH configurations
BACKUP_DIR=/etc/ssh/backup
