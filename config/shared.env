# Shared environment variables for PQR-Tunnel containers

# Docker network configuration
NETWORK_NAME=pqr-network

# Quantum-safe algorithms
DEFAULT_KEY_ALGORITHM=ssh-falcon512
DEFAULT_KEX_ALGORITHM=kyber-512-sha256
DEFAULT_PUBKEY_ALGORITHMS=ssh-falcon512,ssh-rsa

# Host key paths (used by both client and server)
HOST_KEYS_DIR=/etc/ssh/quantum_keys

# Common repository URLs
OQS_SSH_REPO=https://github.com/open-quantum-safe/openssh-portable.git
LIBOQS_REPO=https://github.com/open-quantum-safe/liboqs.git

# Common installation prefix
INSTALL_PREFIX=/usr/local
